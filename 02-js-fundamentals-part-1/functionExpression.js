function addNumbers(a, b) {
  return a + b;
}

res = addNumbers(5, 5);
console.log(res);

let res2 = function (a, b) {
  return a + b;
};

console.log(res2(5, 5));

//It’s another way of using function is js. Result of the function is always stored in a constant or a variable.
