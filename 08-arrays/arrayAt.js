//NOTE at method is the alternative to bracket notation
//It can also be used in strings

const arr = [23, 11, 64];
console.log(arr[arr.length - 1]);
console.log(arr.slice(-1)[0]);
console.log(arr.at(-1));
