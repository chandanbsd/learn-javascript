let age = 18;

const result = age >= 18 ? "Allowed to drink" : "Not allowed to drink";
console.log(result);

let res2;
if (age >= 18) {
  res2 = "Allowed to drink";
} else {
  res2 = "Not allowed to drink";
}

console.log(res2);
