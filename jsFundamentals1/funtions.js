function example() {
  console.log("Hello World");
}

example();

function add(a, b) {
  let res = a + b;
  return res;
}

console.log(add(2, 5));

/*
- Functions allow us to reduce code redundancy, generally referred to as ensuring “dry code”.
- functions may or may not return a value, similarly they may or may not have accept parameters.
*/
