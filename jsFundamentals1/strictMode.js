/* NOTE Strict Mode
strict mode basically lets us know certain errors that otherwise is not show. It may be applied to the program as a whole as shown above or for a function.
*/
"use strict";

let hasDriversLicense = false;
const hasPassedTest = true;

if (hasPassedTest) {
  hasDriverLicense = true;
}

console.log(hasDriversLicense);

// const private = 1; This throws an error
